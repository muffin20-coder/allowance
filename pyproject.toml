[project]
name = "allowance"
version = "0.1.0"
description = "Simple allowance planning and tracking CLI"
authors = [{name = "Generated by ChatGPT"}]
requires-python = ">=3.10"
readme = "README.md"
dependencies = []

[project.scripts]
allowance = "allowance.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
